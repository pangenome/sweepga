name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

env:
  CARGO_TERM_COLOR: always

jobs:
  test:
    name: Test Suite
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
    steps:
      - uses: actions/checkout@v3

      - name: Install Rust
        uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: stable
          override: true
          components: rustfmt, clippy

      - name: Check formatting
        run: cargo fmt -- --check

      - name: Run clippy
        run: cargo clippy -- -D warnings

      - name: Build
        run: cargo build --verbose --all-targets

      - name: Set FastGA binary path
        run: |
          # Find the FastGA binaries built by fastga-rs and add to PATH
          FASTGA_DIR=$(find target/debug/build -name "fastga-rs-*" -type d | head -1)/out
          if [ -d "$FASTGA_DIR" ] && [ -f "$FASTGA_DIR/FastGA" ]; then
            echo "Found FastGA binaries in: $FASTGA_DIR"
            echo "$FASTGA_DIR" >> $GITHUB_PATH
          else
            echo "Warning: FastGA binaries not found in expected location"
          fi

      - name: Run tests
        run: cargo test --verbose

      - name: Set FastGA binary path (release)
        run: |
          # Find the FastGA binaries in release build directory
          FASTGA_DIR=$(find target/release/build -name "fastga-rs-*" -type d | head -1)/out
          if [ -d "$FASTGA_DIR" ] && [ -f "$FASTGA_DIR/FastGA" ]; then
            echo "Found FastGA release binaries in: $FASTGA_DIR"
            echo "$FASTGA_DIR" >> $GITHUB_PATH
          else
            echo "Warning: FastGA release binaries not found, trying to build..."
            cargo build --release
            FASTGA_DIR=$(find target/release/build -name "fastga-rs-*" -type d | head -1)/out
            if [ -d "$FASTGA_DIR" ]; then
              echo "$FASTGA_DIR" >> $GITHUB_PATH
            fi
          fi

      - name: Run tests (release mode)
        run: cargo test --release --verbose

      - name: Check for forbidden files
        run: |
          # Check for files that should never be committed
          if git ls-tree -r HEAD --name-only | grep -E '\.(fa|fasta|paf|gdb|ktab|bps)$' | grep -v 'data/scerevisiae8.fa.gz'; then
            echo "ERROR: Found forbidden data files in repository!"
            echo "Only data/scerevisiae8.fa.gz is allowed"
            exit 1
          fi

  build:
    name: Build Check
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
        rust:
          - stable
          - beta
          - nightly
    steps:
      - uses: actions/checkout@v3

      - name: Install Rust
        uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: ${{ matrix.rust }}
          override: true

      - name: Build
        run: cargo build --verbose

      - name: Build (all features)
        run: cargo build --verbose --all-features